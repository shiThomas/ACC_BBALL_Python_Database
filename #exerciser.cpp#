#include "exerciser.h"

void exercise(connection * C) {
  add_player(C, 3, 89, "Weicheng", "Shi", 35, 30, 20, 10, 10, 10);
  add_team(C, "Dragon", 3, 3, 100, 10);
  add_state(C, "WhatState");
  add_color(C, "SoDark");
  query1(C, 1, 0, 30, 1, 10, 30, 1, 10, 20, 0, 0, 0, 0, 0.0, 5.0, 0, 0.0, 5.0);
  query2(C, "Yellow");
  
  
}
